name: Publish Java Package

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:   # Permite lanzar el workflow manualmente

jobs:
  build:

    runs-on: ubuntu-latest

    # 🔐 Permisos para leer y publicar paquetes
    permissions:
      contents: read
      packages: write

    steps:
      # 1️⃣ Checkout del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configurar JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Configurar Gradle cache
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # 4️⃣ Asegurar permisos de ejecución para el wrapper
      - name: Change wrapper permissions
        run: chmod +x ./gradlew

      # 5️⃣ Compilar el proyecto
      - name: Build with Gradle
        run: ./gradlew clean build
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 6️⃣ Publicar en GitHub Packages
      - name: Publish to GitHub Packages
        run: ./gradlew publish
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
